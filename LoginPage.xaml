<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="http://prismlibrary.com"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="Logintest.Views.LoginPage">
    <StackLayout BackgroundColor="LightGray" Padding="10">
        <Label Text="Login Page" TextColor="Black" FontSize="50" FontAttributes="Bold" HorizontalOptions="Center" TextDecorations="Underline" Margin="0,80,0,0"/>
        <StackLayout Padding="5" HorizontalOptions="CenterAndExpand" VerticalOptions="CenterAndExpand">
            <Frame Padding="5" BackgroundColor="LightCyan">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>

                    </Grid.RowDefinitions>
                    <Entry x:Name="entryuser" Placeholder="username" Grid.Row="0"/>
                    <Entry x:Name="entrypassword" Placeholder="password" IsPassword="True" Grid.Row="1"/>
                    <Button Text="Login" Grid.Row="2" Clicked="Button_Clicked_1" BackgroundColor="#0077be"/>

                </Grid>
            </Frame>
        </StackLayout>
    </StackLayout>

</ContentPage>